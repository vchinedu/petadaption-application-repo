# database init, supports mysql too
database=mysql
spring.datasource.url=${MYSQL_URL:jdbc:mysql://multiazdb.cp600om8mwf1.eu-west-2.rds.amazonaws.com:3306/petclinic}
spring.datasource.username=${MYSQL_USER:admin}
spring.datasource.password=${MYSQL_PASS:jdLwPNQKBc}
spring.datasource.schema=classpath:db/mysql/schema.sql
spring.datasource.data=classpath:db/mysql/data.sql
# SQL is written to be idempotent so this is safe
spring.datasource.initialization-mode=always
